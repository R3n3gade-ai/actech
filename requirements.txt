# =============================================================================
# Achelion ARMS — Python Runtime Dependencies (curated)
# =============================================================================
# Install: pip install -r requirements.txt
# Python: 3.11+ (developed on 3.13)
# For a fully pinned transitive lockfile, see requirements.lock.txt
# =============================================================================

# --- LLM Providers (install at least one matching DEFAULT_LLM_MODEL) ---------
openai==2.33.0
# anthropic>=0.40.0       # uncomment if using Claude models
# google-genai>=0.5.0     # uncomment if using Gemini models

# --- Broker / Market Data ---------------------------------------------------
ib-insync==0.9.86
yfinance==1.3.0

# --- HTTP / Parsing ---------------------------------------------------------
requests==2.33.1
beautifulsoup4==4.14.3
lxml==6.1.0
feedparser==6.0.12

# --- Data / Analytics -------------------------------------------------------
pandas==3.0.2
numpy==2.4.4

# --- Scheduling / Async -----------------------------------------------------
APScheduler==3.11.2
nest-asyncio==1.6.0

# --- AWS (production deployment) --------------------------------------------
boto3==1.42.97

# --- Config -----------------------------------------------------------------
python-dotenv==1.2.2

# --- Optional production extras (uncomment if using Postgres/Redis) ---------
# psycopg2-binary>=2.9.9
# redis>=5.0.0
